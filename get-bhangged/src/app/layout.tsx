import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "getBhangged | Sacred Origins & Bhang Stories",
  description: "Explore the mystical roots, rituals, and stories behind Bhang in India. From Lord Shiva's meditation to the vibrant colors of Holi.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
